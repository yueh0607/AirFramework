仅用作开发过程记录，以及未来版本preview

# 未来可能实现/支持的内容(放在这里防止我忘)：
- MVC/PanelManager支持的UI管理器(配备代码自动生成)
- 热更新与Hotfix (HybirdCLR or XLua or ILRuntime)
- 简单网络模块(TCP/UDP/HTTP支持)
- 代码状态机/行为树
- csv/json/binary/excel/string等序列化
- SDK管理器与快捷接入
- 游戏AI创建支持(深度学习/机器学习)
- 多语言管理
- 按键绑定/触控/手势管理
- Tween模块
- 编辑器拓展强化支持
- 将Addressable替换为yooAssest


数学类：
  1.求各种点到面，线到面，点到线，面到面的距离
  2.求各种三角形面积，多边形面积
  3.贝塞尔，二次函数，指对函数，等各种曲线
  4.范围判断，多边形坐标判断
  5.二值化
动画：
  1.提供常见的Dotween动画
  2.封装出UI常见动画，如平移进入，伤害数字，放大进入等
资源：
  1.提供同步加载方式和异步加载方式
  2.采用引用计数方式
  3.采用全局管理器
UI：
  1.自动生成View层代码
  2.自动生成Controller的事件并绑定
